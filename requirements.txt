# DisTraceAI runtime requirements (single "distrace" env).
# vLLM 0.22 brings its own matched torch (2.11) + CUDA 13.0 runtime and pulls
# transformers>=5, so we do NOT pin torch here. transformers is left unpinned so
# vLLM resolves a v5 that supports Qwen3.5 / Gemma 4; the check-worthiness
# detectors (mDeBERTa / XLM-R) load fine under that same transformers v5, so a
# single environment serves the whole pipeline.

# inference: one library for LLM generation + embeddings.
vllm==0.22.1

# retrieval
# NodeRAG is NOT installed from PyPI — it's a patched local clone installed by
# modules/noderag/install_noderag_local.sh (called from setup.sh). Listing it
# here would either fail to resolve or install an UNPATCHED copy that conflicts
# with the editable install, and a failed line aborts the rest of this file.
chromadb

# core / pipeline
numpy
pandas
scikit-learn
rank-bm25
feedparser
trafilatura
fasttext-langdetect
bertopic

# hub downloads (faster, more robust large-file transfers)
hf_transfer

# eval / utils
rich
pyyaml

# dev / testing
pytest
